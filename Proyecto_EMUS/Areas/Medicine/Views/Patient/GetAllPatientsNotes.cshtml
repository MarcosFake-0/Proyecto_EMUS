@model List<ClinicalHistoryNote>

<partial name="_Notifications" />

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Notas paciente
            </h2>
        </div>
        @* <div class="col-6 text-end">
            <a asp-controller="Patient" asp-action="UpsertNote" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> Crear nueva nota</a>
        </div> *@
    </div>

    <table class="table table-dark table-hover">
        <thead>
            <tr class="table-primary">
                <th>Descripción</th>
                <th>Fecha creación</th>
               @*  <th>Actions</th> *@
            </tr>
        </thead>

        <tbody>
            @foreach (var thisClinicalHistoryNote in Model)
            {
                <tr class="table-info">
                    <td>@thisClinicalHistoryNote.Description</td>
                    <td>@thisClinicalHistoryNote.CreatedAt</td>
                    @* <td>
                        <div class="btn-group w-75" role="group">
                            <a asp-controller="Patient" asp-action="UpsertNote" asp-route-id="@thisClinicalHistoryNote.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil"></i>
                            </a>
                     
                        </div>
                    </td> *@
                </tr>
            }
        </tbody>
    </table>
</div>
